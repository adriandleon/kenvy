import org.gradle.api.Plugin
import org.gradle.api.Project

class KenvyKotlinPlugin : Plugin<Project> {
    override fun apply(target: Project) {
        // Convention plugin for Kotlin configuration
        // Will be extended in future stories
    }
}
